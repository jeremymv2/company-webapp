server {
listen   <%= node['company-webapp']['port'] %>;
  server_name  <%= node['hostname'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/localhost.access.log;

  location / {
  root   <%= node['company-webapp']['docroot'] %>;
  index  <%= node['company-webapp']['index'] %>;
  }
}